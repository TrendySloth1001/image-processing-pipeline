insightface==0.7.3
onnxruntime==1.30.0
# insightface 0.7.3 predates numpy 2, so stay on numpy 1.x; opencv 4.12+ requires numpy 2.
numpy==1.26.4
opencv-python-headless==4.11.0.86
Pillow==12.3.0
pillow-heif==1.7.0
# Video decoding. The wheels carry their own FFmpeg, so nothing is installed with apt.
av==18.1.0
scikit-learn==1.9.1
psycopg[binary]==3.3.5
pgvector==0.5.0

# Service: queue in, webhook out
redis==8.1.0
boto3==1.43.93
httpx==0.28.1

# HTTP layer (producer API and the folder-based test UI)
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32
jinja2==3.1.6
